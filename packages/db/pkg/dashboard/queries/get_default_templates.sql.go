// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: get_default_templates.sql

package dashboardqueries

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const getDefaultTemplates = `-- name: GetDefaultTemplates :many
SELECT
    e.id AS template_id,
    e.created_at,
    e.public,
    e.build_count,
    e.spawn_count,
    b.id AS build_id,
    b.ram_mb,
    b.vcpu,
    b.total_disk_size_mb,
    b.envd_version
FROM public.env_defaults ed
JOIN public.envs e ON e.id = ed.env_id
JOIN LATERAL (
    SELECT a.build_id
    FROM public.env_build_assignments a
    JOIN public.env_builds eb ON eb.id = a.build_id
    WHERE a.env_id = e.id
      AND eb.status = 'uploaded'
    ORDER BY a.created_at DESC, a.id DESC
    LIMIT 1
) latest_assignment ON TRUE
JOIN public.env_builds b ON b.id = latest_assignment.build_id
`

type GetDefaultTemplatesRow struct {
	TemplateID      string
	CreatedAt       time.Time
	Public          bool
	BuildCount      int32
	SpawnCount      int64
	BuildID         uuid.UUID
	RamMb           int64
	Vcpu            int64
	TotalDiskSizeMb *int64
	EnvdVersion     *string
}

func (q *Queries) GetDefaultTemplates(ctx context.Context) ([]GetDefaultTemplatesRow, error) {
	rows, err := q.db.Query(ctx, getDefaultTemplates)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetDefaultTemplatesRow
	for rows.Next() {
		var i GetDefaultTemplatesRow
		if err := rows.Scan(
			&i.TemplateID,
			&i.CreatedAt,
			&i.Public,
			&i.BuildCount,
			&i.SpawnCount,
			&i.BuildID,
			&i.RamMb,
			&i.Vcpu,
			&i.TotalDiskSizeMb,
			&i.EnvdVersion,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
