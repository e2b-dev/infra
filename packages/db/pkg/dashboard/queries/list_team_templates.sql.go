// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: list_team_templates.sql

package dashboardqueries

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const listTeamTemplatesByCpuCountAsc = `-- name: ListTeamTemplatesByCpuCountAsc :many
SELECT
    e.id AS template_id,
    e.created_at,
    e.updated_at,
    e.public,
    e.build_count,
    e.spawn_count,
    e.last_spawned_at,
    e.created_by AS creator_id,
    COALESCE(eb.id, '00000000-0000-0000-0000-000000000000'::uuid) AS build_id,
    COALESCE(eb.vcpu, 0)::bigint AS cpu_count,
    COALESCE(eb.ram_mb, 0)::bigint AS memory_mb,
    eb.total_disk_size_mb AS disk_size_mb,
    eb.envd_version AS envd_version,
    COALESCE(ea.aliases, ARRAY[]::text[])::text[] AS aliases,
    COALESCE(ea.names, ARRAY[]::text[])::text[] AS names,
    (d.env_id IS NOT NULL)::boolean AS is_default,
    d.description AS default_description,
    COALESCE(ea.name_sort_key, '')::text AS name_sort_key
FROM public.envs AS e
LEFT JOIN public.env_defaults AS d ON d.env_id = e.id
LEFT JOIN LATERAL (
    SELECT
        ARRAY_AGG(alias ORDER BY alias) AS aliases,
        ARRAY_AGG(CASE WHEN namespace IS NOT NULL THEN namespace || '/' || alias ELSE alias END ORDER BY alias) AS names,
        MIN(CASE WHEN namespace IS NOT NULL THEN namespace || '/' || alias ELSE alias END) AS name_sort_key
    FROM public.env_aliases
    WHERE env_id = e.id
) ea ON TRUE
LEFT JOIN LATERAL (
    SELECT b.id, b.vcpu, b.ram_mb, b.total_disk_size_mb, b.envd_version
    FROM public.env_build_assignments AS ba
    JOIN public.env_builds AS b ON b.id = ba.build_id
    WHERE ba.env_id = e.id AND ba.tag = 'default' AND b.status_group = 'ready'
    ORDER BY ba.created_at DESC
    LIMIT 1
) eb ON TRUE
WHERE
    (
        (e.team_id = $1::uuid AND e.source = 'template')
        OR ($2::boolean AND d.env_id IS NOT NULL)
    )
    AND ($3::bigint = 0 OR COALESCE(eb.vcpu, 0) = $3::bigint)
    AND ($4::bigint = 0 OR COALESCE(eb.ram_mb, 0) = $4::bigint)
    AND ($5::smallint = -1 OR e.public = ($5::smallint = 1))
    AND (
        $6::text = ''
        OR e.id ILIKE '%' || $6::text || '%'
        OR EXISTS (
            SELECT 1 FROM public.env_aliases sa
            WHERE sa.env_id = e.id
              AND (
                sa.alias ILIKE '%' || $6::text || '%'
                OR COALESCE(sa.namespace, '') || '/' || sa.alias ILIKE '%' || $6::text || '%'
              )
        )
    )
    AND (
        $7::bigint IS NULL
        OR COALESCE(eb.vcpu, 0) > $7::bigint
        OR (COALESCE(eb.vcpu, 0) = $7::bigint AND e.id > $8::text)
    )
ORDER BY COALESCE(eb.vcpu, 0) ASC, e.id ASC
LIMIT $9::int
`

type ListTeamTemplatesByCpuCountAscParams struct {
	TeamID          uuid.UUID
	IncludeDefaults bool
	CpuCount        int64
	MemoryMb        int64
	FilterPublic    int16
	Search          string
	CursorCpuCount  *int64
	CursorID        *string
	LimitPlusOne    int32
}

type ListTeamTemplatesByCpuCountAscRow struct {
	TemplateID         string
	CreatedAt          time.Time
	UpdatedAt          time.Time
	Public             bool
	BuildCount         int32
	SpawnCount         int64
	LastSpawnedAt      *time.Time
	CreatorID          *uuid.UUID
	BuildID            uuid.UUID
	CpuCount           int64
	MemoryMb           int64
	DiskSizeMb         *int64
	EnvdVersion        *string
	Aliases            []string
	Names              []string
	IsDefault          bool
	DefaultDescription *string
	NameSortKey        string
}

func (q *Queries) ListTeamTemplatesByCpuCountAsc(ctx context.Context, arg ListTeamTemplatesByCpuCountAscParams) ([]ListTeamTemplatesByCpuCountAscRow, error) {
	rows, err := q.db.Query(ctx, listTeamTemplatesByCpuCountAsc,
		arg.TeamID,
		arg.IncludeDefaults,
		arg.CpuCount,
		arg.MemoryMb,
		arg.FilterPublic,
		arg.Search,
		arg.CursorCpuCount,
		arg.CursorID,
		arg.LimitPlusOne,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListTeamTemplatesByCpuCountAscRow
	for rows.Next() {
		var i ListTeamTemplatesByCpuCountAscRow
		if err := rows.Scan(
			&i.TemplateID,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.Public,
			&i.BuildCount,
			&i.SpawnCount,
			&i.LastSpawnedAt,
			&i.CreatorID,
			&i.BuildID,
			&i.CpuCount,
			&i.MemoryMb,
			&i.DiskSizeMb,
			&i.EnvdVersion,
			&i.Aliases,
			&i.Names,
			&i.IsDefault,
			&i.DefaultDescription,
			&i.NameSortKey,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listTeamTemplatesByCpuCountDesc = `-- name: ListTeamTemplatesByCpuCountDesc :many
SELECT
    e.id AS template_id,
    e.created_at,
    e.updated_at,
    e.public,
    e.build_count,
    e.spawn_count,
    e.last_spawned_at,
    e.created_by AS creator_id,
    COALESCE(eb.id, '00000000-0000-0000-0000-000000000000'::uuid) AS build_id,
    COALESCE(eb.vcpu, 0)::bigint AS cpu_count,
    COALESCE(eb.ram_mb, 0)::bigint AS memory_mb,
    eb.total_disk_size_mb AS disk_size_mb,
    eb.envd_version AS envd_version,
    COALESCE(ea.aliases, ARRAY[]::text[])::text[] AS aliases,
    COALESCE(ea.names, ARRAY[]::text[])::text[] AS names,
    (d.env_id IS NOT NULL)::boolean AS is_default,
    d.description AS default_description,
    COALESCE(ea.name_sort_key, '')::text AS name_sort_key
FROM public.envs AS e
LEFT JOIN public.env_defaults AS d ON d.env_id = e.id
LEFT JOIN LATERAL (
    SELECT
        ARRAY_AGG(alias ORDER BY alias) AS aliases,
        ARRAY_AGG(CASE WHEN namespace IS NOT NULL THEN namespace || '/' || alias ELSE alias END ORDER BY alias) AS names,
        MIN(CASE WHEN namespace IS NOT NULL THEN namespace || '/' || alias ELSE alias END) AS name_sort_key
    FROM public.env_aliases
    WHERE env_id = e.id
) ea ON TRUE
LEFT JOIN LATERAL (
    SELECT b.id, b.vcpu, b.ram_mb, b.total_disk_size_mb, b.envd_version
    FROM public.env_build_assignments AS ba
    JOIN public.env_builds AS b ON b.id = ba.build_id
    WHERE ba.env_id = e.id AND ba.tag = 'default' AND b.status_group = 'ready'
    ORDER BY ba.created_at DESC
    LIMIT 1
) eb ON TRUE
WHERE
    (
        (e.team_id = $1::uuid AND e.source = 'template')
        OR ($2::boolean AND d.env_id IS NOT NULL)
    )
    AND ($3::bigint = 0 OR COALESCE(eb.vcpu, 0) = $3::bigint)
    AND ($4::bigint = 0 OR COALESCE(eb.ram_mb, 0) = $4::bigint)
    AND ($5::smallint = -1 OR e.public = ($5::smallint = 1))
    AND (
        $6::text = ''
        OR e.id ILIKE '%' || $6::text || '%'
        OR EXISTS (
            SELECT 1 FROM public.env_aliases sa
            WHERE sa.env_id = e.id
              AND (
                sa.alias ILIKE '%' || $6::text || '%'
                OR COALESCE(sa.namespace, '') || '/' || sa.alias ILIKE '%' || $6::text || '%'
              )
        )
    )
    AND (
        $7::bigint IS NULL
        OR COALESCE(eb.vcpu, 0) < $7::bigint
        OR (COALESCE(eb.vcpu, 0) = $7::bigint AND e.id > $8::text)
    )
ORDER BY COALESCE(eb.vcpu, 0) DESC, e.id ASC
LIMIT $9::int
`

type ListTeamTemplatesByCpuCountDescParams struct {
	TeamID          uuid.UUID
	IncludeDefaults bool
	CpuCount        int64
	MemoryMb        int64
	FilterPublic    int16
	Search          string
	CursorCpuCount  *int64
	CursorID        *string
	LimitPlusOne    int32
}

type ListTeamTemplatesByCpuCountDescRow struct {
	TemplateID         string
	CreatedAt          time.Time
	UpdatedAt          time.Time
	Public             bool
	BuildCount         int32
	SpawnCount         int64
	LastSpawnedAt      *time.Time
	CreatorID          *uuid.UUID
	BuildID            uuid.UUID
	CpuCount           int64
	MemoryMb           int64
	DiskSizeMb         *int64
	EnvdVersion        *string
	Aliases            []string
	Names              []string
	IsDefault          bool
	DefaultDescription *string
	NameSortKey        string
}

func (q *Queries) ListTeamTemplatesByCpuCountDesc(ctx context.Context, arg ListTeamTemplatesByCpuCountDescParams) ([]ListTeamTemplatesByCpuCountDescRow, error) {
	rows, err := q.db.Query(ctx, listTeamTemplatesByCpuCountDesc,
		arg.TeamID,
		arg.IncludeDefaults,
		arg.CpuCount,
		arg.MemoryMb,
		arg.FilterPublic,
		arg.Search,
		arg.CursorCpuCount,
		arg.CursorID,
		arg.LimitPlusOne,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListTeamTemplatesByCpuCountDescRow
	for rows.Next() {
		var i ListTeamTemplatesByCpuCountDescRow
		if err := rows.Scan(
			&i.TemplateID,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.Public,
			&i.BuildCount,
			&i.SpawnCount,
			&i.LastSpawnedAt,
			&i.CreatorID,
			&i.BuildID,
			&i.CpuCount,
			&i.MemoryMb,
			&i.DiskSizeMb,
			&i.EnvdVersion,
			&i.Aliases,
			&i.Names,
			&i.IsDefault,
			&i.DefaultDescription,
			&i.NameSortKey,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listTeamTemplatesByCreatedAtAsc = `-- name: ListTeamTemplatesByCreatedAtAsc :many
SELECT
    e.id AS template_id,
    e.created_at,
    e.updated_at,
    e.public,
    e.build_count,
    e.spawn_count,
    e.last_spawned_at,
    e.created_by AS creator_id,
    COALESCE(eb.id, '00000000-0000-0000-0000-000000000000'::uuid) AS build_id,
    COALESCE(eb.vcpu, 0)::bigint AS cpu_count,
    COALESCE(eb.ram_mb, 0)::bigint AS memory_mb,
    eb.total_disk_size_mb AS disk_size_mb,
    eb.envd_version AS envd_version,
    COALESCE(ea.aliases, ARRAY[]::text[])::text[] AS aliases,
    COALESCE(ea.names, ARRAY[]::text[])::text[] AS names,
    (d.env_id IS NOT NULL)::boolean AS is_default,
    d.description AS default_description,
    COALESCE(ea.name_sort_key, '')::text AS name_sort_key
FROM public.envs AS e
LEFT JOIN public.env_defaults AS d ON d.env_id = e.id
LEFT JOIN LATERAL (
    SELECT
        ARRAY_AGG(alias ORDER BY alias) AS aliases,
        ARRAY_AGG(CASE WHEN namespace IS NOT NULL THEN namespace || '/' || alias ELSE alias END ORDER BY alias) AS names,
        MIN(CASE WHEN namespace IS NOT NULL THEN namespace || '/' || alias ELSE alias END) AS name_sort_key
    FROM public.env_aliases
    WHERE env_id = e.id
) ea ON TRUE
LEFT JOIN LATERAL (
    SELECT b.id, b.vcpu, b.ram_mb, b.total_disk_size_mb, b.envd_version
    FROM public.env_build_assignments AS ba
    JOIN public.env_builds AS b ON b.id = ba.build_id
    WHERE ba.env_id = e.id AND ba.tag = 'default' AND b.status_group = 'ready'
    ORDER BY ba.created_at DESC
    LIMIT 1
) eb ON TRUE
WHERE
    (
        (e.team_id = $1::uuid AND e.source = 'template')
        OR ($2::boolean AND d.env_id IS NOT NULL)
    )
    AND ($3::bigint = 0 OR COALESCE(eb.vcpu, 0) = $3::bigint)
    AND ($4::bigint = 0 OR COALESCE(eb.ram_mb, 0) = $4::bigint)
    AND ($5::smallint = -1 OR e.public = ($5::smallint = 1))
    AND (
        $6::text = ''
        OR e.id ILIKE '%' || $6::text || '%'
        OR EXISTS (
            SELECT 1 FROM public.env_aliases sa
            WHERE sa.env_id = e.id
              AND (
                sa.alias ILIKE '%' || $6::text || '%'
                OR COALESCE(sa.namespace, '') || '/' || sa.alias ILIKE '%' || $6::text || '%'
              )
        )
    )
    AND (
        $7::timestamptz IS NULL
        OR e.created_at > $7::timestamptz
        OR (e.created_at = $7::timestamptz AND e.id > $8::text)
    )
ORDER BY e.created_at ASC, e.id ASC
LIMIT $9::int
`

type ListTeamTemplatesByCreatedAtAscParams struct {
	TeamID          uuid.UUID
	IncludeDefaults bool
	CpuCount        int64
	MemoryMb        int64
	FilterPublic    int16
	Search          string
	CursorCreatedAt *time.Time
	CursorID        *string
	LimitPlusOne    int32
}

type ListTeamTemplatesByCreatedAtAscRow struct {
	TemplateID         string
	CreatedAt          time.Time
	UpdatedAt          time.Time
	Public             bool
	BuildCount         int32
	SpawnCount         int64
	LastSpawnedAt      *time.Time
	CreatorID          *uuid.UUID
	BuildID            uuid.UUID
	CpuCount           int64
	MemoryMb           int64
	DiskSizeMb         *int64
	EnvdVersion        *string
	Aliases            []string
	Names              []string
	IsDefault          bool
	DefaultDescription *string
	NameSortKey        string
}

func (q *Queries) ListTeamTemplatesByCreatedAtAsc(ctx context.Context, arg ListTeamTemplatesByCreatedAtAscParams) ([]ListTeamTemplatesByCreatedAtAscRow, error) {
	rows, err := q.db.Query(ctx, listTeamTemplatesByCreatedAtAsc,
		arg.TeamID,
		arg.IncludeDefaults,
		arg.CpuCount,
		arg.MemoryMb,
		arg.FilterPublic,
		arg.Search,
		arg.CursorCreatedAt,
		arg.CursorID,
		arg.LimitPlusOne,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListTeamTemplatesByCreatedAtAscRow
	for rows.Next() {
		var i ListTeamTemplatesByCreatedAtAscRow
		if err := rows.Scan(
			&i.TemplateID,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.Public,
			&i.BuildCount,
			&i.SpawnCount,
			&i.LastSpawnedAt,
			&i.CreatorID,
			&i.BuildID,
			&i.CpuCount,
			&i.MemoryMb,
			&i.DiskSizeMb,
			&i.EnvdVersion,
			&i.Aliases,
			&i.Names,
			&i.IsDefault,
			&i.DefaultDescription,
			&i.NameSortKey,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listTeamTemplatesByCreatedAtDesc = `-- name: ListTeamTemplatesByCreatedAtDesc :many
SELECT
    e.id AS template_id,
    e.created_at,
    e.updated_at,
    e.public,
    e.build_count,
    e.spawn_count,
    e.last_spawned_at,
    e.created_by AS creator_id,
    COALESCE(eb.id, '00000000-0000-0000-0000-000000000000'::uuid) AS build_id,
    COALESCE(eb.vcpu, 0)::bigint AS cpu_count,
    COALESCE(eb.ram_mb, 0)::bigint AS memory_mb,
    eb.total_disk_size_mb AS disk_size_mb,
    eb.envd_version AS envd_version,
    COALESCE(ea.aliases, ARRAY[]::text[])::text[] AS aliases,
    COALESCE(ea.names, ARRAY[]::text[])::text[] AS names,
    (d.env_id IS NOT NULL)::boolean AS is_default,
    d.description AS default_description,
    COALESCE(ea.name_sort_key, '')::text AS name_sort_key
FROM public.envs AS e
LEFT JOIN public.env_defaults AS d ON d.env_id = e.id
LEFT JOIN LATERAL (
    SELECT
        ARRAY_AGG(alias ORDER BY alias) AS aliases,
        ARRAY_AGG(CASE WHEN namespace IS NOT NULL THEN namespace || '/' || alias ELSE alias END ORDER BY alias) AS names,
        MIN(CASE WHEN namespace IS NOT NULL THEN namespace || '/' || alias ELSE alias END) AS name_sort_key
    FROM public.env_aliases
    WHERE env_id = e.id
) ea ON TRUE
LEFT JOIN LATERAL (
    SELECT b.id, b.vcpu, b.ram_mb, b.total_disk_size_mb, b.envd_version
    FROM public.env_build_assignments AS ba
    JOIN public.env_builds AS b ON b.id = ba.build_id
    WHERE ba.env_id = e.id AND ba.tag = 'default' AND b.status_group = 'ready'
    ORDER BY ba.created_at DESC
    LIMIT 1
) eb ON TRUE
WHERE
    (
        (e.team_id = $1::uuid AND e.source = 'template')
        OR ($2::boolean AND d.env_id IS NOT NULL)
    )
    AND ($3::bigint = 0 OR COALESCE(eb.vcpu, 0) = $3::bigint)
    AND ($4::bigint = 0 OR COALESCE(eb.ram_mb, 0) = $4::bigint)
    AND ($5::smallint = -1 OR e.public = ($5::smallint = 1))
    AND (
        $6::text = ''
        OR e.id ILIKE '%' || $6::text || '%'
        OR EXISTS (
            SELECT 1 FROM public.env_aliases sa
            WHERE sa.env_id = e.id
              AND (
                sa.alias ILIKE '%' || $6::text || '%'
                OR COALESCE(sa.namespace, '') || '/' || sa.alias ILIKE '%' || $6::text || '%'
              )
        )
    )
    AND (
        $7::timestamptz IS NULL
        OR e.created_at < $7::timestamptz
        OR (e.created_at = $7::timestamptz AND e.id > $8::text)
    )
ORDER BY e.created_at DESC, e.id ASC
LIMIT $9::int
`

type ListTeamTemplatesByCreatedAtDescParams struct {
	TeamID          uuid.UUID
	IncludeDefaults bool
	CpuCount        int64
	MemoryMb        int64
	FilterPublic    int16
	Search          string
	CursorCreatedAt *time.Time
	CursorID        *string
	LimitPlusOne    int32
}

type ListTeamTemplatesByCreatedAtDescRow struct {
	TemplateID         string
	CreatedAt          time.Time
	UpdatedAt          time.Time
	Public             bool
	BuildCount         int32
	SpawnCount         int64
	LastSpawnedAt      *time.Time
	CreatorID          *uuid.UUID
	BuildID            uuid.UUID
	CpuCount           int64
	MemoryMb           int64
	DiskSizeMb         *int64
	EnvdVersion        *string
	Aliases            []string
	Names              []string
	IsDefault          bool
	DefaultDescription *string
	NameSortKey        string
}

func (q *Queries) ListTeamTemplatesByCreatedAtDesc(ctx context.Context, arg ListTeamTemplatesByCreatedAtDescParams) ([]ListTeamTemplatesByCreatedAtDescRow, error) {
	rows, err := q.db.Query(ctx, listTeamTemplatesByCreatedAtDesc,
		arg.TeamID,
		arg.IncludeDefaults,
		arg.CpuCount,
		arg.MemoryMb,
		arg.FilterPublic,
		arg.Search,
		arg.CursorCreatedAt,
		arg.CursorID,
		arg.LimitPlusOne,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListTeamTemplatesByCreatedAtDescRow
	for rows.Next() {
		var i ListTeamTemplatesByCreatedAtDescRow
		if err := rows.Scan(
			&i.TemplateID,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.Public,
			&i.BuildCount,
			&i.SpawnCount,
			&i.LastSpawnedAt,
			&i.CreatorID,
			&i.BuildID,
			&i.CpuCount,
			&i.MemoryMb,
			&i.DiskSizeMb,
			&i.EnvdVersion,
			&i.Aliases,
			&i.Names,
			&i.IsDefault,
			&i.DefaultDescription,
			&i.NameSortKey,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listTeamTemplatesByMemoryMbAsc = `-- name: ListTeamTemplatesByMemoryMbAsc :many
SELECT
    e.id AS template_id,
    e.created_at,
    e.updated_at,
    e.public,
    e.build_count,
    e.spawn_count,
    e.last_spawned_at,
    e.created_by AS creator_id,
    COALESCE(eb.id, '00000000-0000-0000-0000-000000000000'::uuid) AS build_id,
    COALESCE(eb.vcpu, 0)::bigint AS cpu_count,
    COALESCE(eb.ram_mb, 0)::bigint AS memory_mb,
    eb.total_disk_size_mb AS disk_size_mb,
    eb.envd_version AS envd_version,
    COALESCE(ea.aliases, ARRAY[]::text[])::text[] AS aliases,
    COALESCE(ea.names, ARRAY[]::text[])::text[] AS names,
    (d.env_id IS NOT NULL)::boolean AS is_default,
    d.description AS default_description,
    COALESCE(ea.name_sort_key, '')::text AS name_sort_key
FROM public.envs AS e
LEFT JOIN public.env_defaults AS d ON d.env_id = e.id
LEFT JOIN LATERAL (
    SELECT
        ARRAY_AGG(alias ORDER BY alias) AS aliases,
        ARRAY_AGG(CASE WHEN namespace IS NOT NULL THEN namespace || '/' || alias ELSE alias END ORDER BY alias) AS names,
        MIN(CASE WHEN namespace IS NOT NULL THEN namespace || '/' || alias ELSE alias END) AS name_sort_key
    FROM public.env_aliases
    WHERE env_id = e.id
) ea ON TRUE
LEFT JOIN LATERAL (
    SELECT b.id, b.vcpu, b.ram_mb, b.total_disk_size_mb, b.envd_version
    FROM public.env_build_assignments AS ba
    JOIN public.env_builds AS b ON b.id = ba.build_id
    WHERE ba.env_id = e.id AND ba.tag = 'default' AND b.status_group = 'ready'
    ORDER BY ba.created_at DESC
    LIMIT 1
) eb ON TRUE
WHERE
    (
        (e.team_id = $1::uuid AND e.source = 'template')
        OR ($2::boolean AND d.env_id IS NOT NULL)
    )
    AND ($3::bigint = 0 OR COALESCE(eb.vcpu, 0) = $3::bigint)
    AND ($4::bigint = 0 OR COALESCE(eb.ram_mb, 0) = $4::bigint)
    AND ($5::smallint = -1 OR e.public = ($5::smallint = 1))
    AND (
        $6::text = ''
        OR e.id ILIKE '%' || $6::text || '%'
        OR EXISTS (
            SELECT 1 FROM public.env_aliases sa
            WHERE sa.env_id = e.id
              AND (
                sa.alias ILIKE '%' || $6::text || '%'
                OR COALESCE(sa.namespace, '') || '/' || sa.alias ILIKE '%' || $6::text || '%'
              )
        )
    )
    AND (
        $7::bigint IS NULL
        OR COALESCE(eb.ram_mb, 0) > $7::bigint
        OR (COALESCE(eb.ram_mb, 0) = $7::bigint AND e.id > $8::text)
    )
ORDER BY COALESCE(eb.ram_mb, 0) ASC, e.id ASC
LIMIT $9::int
`

type ListTeamTemplatesByMemoryMbAscParams struct {
	TeamID          uuid.UUID
	IncludeDefaults bool
	CpuCount        int64
	MemoryMb        int64
	FilterPublic    int16
	Search          string
	CursorMemoryMb  *int64
	CursorID        *string
	LimitPlusOne    int32
}

type ListTeamTemplatesByMemoryMbAscRow struct {
	TemplateID         string
	CreatedAt          time.Time
	UpdatedAt          time.Time
	Public             bool
	BuildCount         int32
	SpawnCount         int64
	LastSpawnedAt      *time.Time
	CreatorID          *uuid.UUID
	BuildID            uuid.UUID
	CpuCount           int64
	MemoryMb           int64
	DiskSizeMb         *int64
	EnvdVersion        *string
	Aliases            []string
	Names              []string
	IsDefault          bool
	DefaultDescription *string
	NameSortKey        string
}

func (q *Queries) ListTeamTemplatesByMemoryMbAsc(ctx context.Context, arg ListTeamTemplatesByMemoryMbAscParams) ([]ListTeamTemplatesByMemoryMbAscRow, error) {
	rows, err := q.db.Query(ctx, listTeamTemplatesByMemoryMbAsc,
		arg.TeamID,
		arg.IncludeDefaults,
		arg.CpuCount,
		arg.MemoryMb,
		arg.FilterPublic,
		arg.Search,
		arg.CursorMemoryMb,
		arg.CursorID,
		arg.LimitPlusOne,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListTeamTemplatesByMemoryMbAscRow
	for rows.Next() {
		var i ListTeamTemplatesByMemoryMbAscRow
		if err := rows.Scan(
			&i.TemplateID,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.Public,
			&i.BuildCount,
			&i.SpawnCount,
			&i.LastSpawnedAt,
			&i.CreatorID,
			&i.BuildID,
			&i.CpuCount,
			&i.MemoryMb,
			&i.DiskSizeMb,
			&i.EnvdVersion,
			&i.Aliases,
			&i.Names,
			&i.IsDefault,
			&i.DefaultDescription,
			&i.NameSortKey,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listTeamTemplatesByMemoryMbDesc = `-- name: ListTeamTemplatesByMemoryMbDesc :many
SELECT
    e.id AS template_id,
    e.created_at,
    e.updated_at,
    e.public,
    e.build_count,
    e.spawn_count,
    e.last_spawned_at,
    e.created_by AS creator_id,
    COALESCE(eb.id, '00000000-0000-0000-0000-000000000000'::uuid) AS build_id,
    COALESCE(eb.vcpu, 0)::bigint AS cpu_count,
    COALESCE(eb.ram_mb, 0)::bigint AS memory_mb,
    eb.total_disk_size_mb AS disk_size_mb,
    eb.envd_version AS envd_version,
    COALESCE(ea.aliases, ARRAY[]::text[])::text[] AS aliases,
    COALESCE(ea.names, ARRAY[]::text[])::text[] AS names,
    (d.env_id IS NOT NULL)::boolean AS is_default,
    d.description AS default_description,
    COALESCE(ea.name_sort_key, '')::text AS name_sort_key
FROM public.envs AS e
LEFT JOIN public.env_defaults AS d ON d.env_id = e.id
LEFT JOIN LATERAL (
    SELECT
        ARRAY_AGG(alias ORDER BY alias) AS aliases,
        ARRAY_AGG(CASE WHEN namespace IS NOT NULL THEN namespace || '/' || alias ELSE alias END ORDER BY alias) AS names,
        MIN(CASE WHEN namespace IS NOT NULL THEN namespace || '/' || alias ELSE alias END) AS name_sort_key
    FROM public.env_aliases
    WHERE env_id = e.id
) ea ON TRUE
LEFT JOIN LATERAL (
    SELECT b.id, b.vcpu, b.ram_mb, b.total_disk_size_mb, b.envd_version
    FROM public.env_build_assignments AS ba
    JOIN public.env_builds AS b ON b.id = ba.build_id
    WHERE ba.env_id = e.id AND ba.tag = 'default' AND b.status_group = 'ready'
    ORDER BY ba.created_at DESC
    LIMIT 1
) eb ON TRUE
WHERE
    (
        (e.team_id = $1::uuid AND e.source = 'template')
        OR ($2::boolean AND d.env_id IS NOT NULL)
    )
    AND ($3::bigint = 0 OR COALESCE(eb.vcpu, 0) = $3::bigint)
    AND ($4::bigint = 0 OR COALESCE(eb.ram_mb, 0) = $4::bigint)
    AND ($5::smallint = -1 OR e.public = ($5::smallint = 1))
    AND (
        $6::text = ''
        OR e.id ILIKE '%' || $6::text || '%'
        OR EXISTS (
            SELECT 1 FROM public.env_aliases sa
            WHERE sa.env_id = e.id
              AND (
                sa.alias ILIKE '%' || $6::text || '%'
                OR COALESCE(sa.namespace, '') || '/' || sa.alias ILIKE '%' || $6::text || '%'
              )
        )
    )
    AND (
        $7::bigint IS NULL
        OR COALESCE(eb.ram_mb, 0) < $7::bigint
        OR (COALESCE(eb.ram_mb, 0) = $7::bigint AND e.id > $8::text)
    )
ORDER BY COALESCE(eb.ram_mb, 0) DESC, e.id ASC
LIMIT $9::int
`

type ListTeamTemplatesByMemoryMbDescParams struct {
	TeamID          uuid.UUID
	IncludeDefaults bool
	CpuCount        int64
	MemoryMb        int64
	FilterPublic    int16
	Search          string
	CursorMemoryMb  *int64
	CursorID        *string
	LimitPlusOne    int32
}

type ListTeamTemplatesByMemoryMbDescRow struct {
	TemplateID         string
	CreatedAt          time.Time
	UpdatedAt          time.Time
	Public             bool
	BuildCount         int32
	SpawnCount         int64
	LastSpawnedAt      *time.Time
	CreatorID          *uuid.UUID
	BuildID            uuid.UUID
	CpuCount           int64
	MemoryMb           int64
	DiskSizeMb         *int64
	EnvdVersion        *string
	Aliases            []string
	Names              []string
	IsDefault          bool
	DefaultDescription *string
	NameSortKey        string
}

func (q *Queries) ListTeamTemplatesByMemoryMbDesc(ctx context.Context, arg ListTeamTemplatesByMemoryMbDescParams) ([]ListTeamTemplatesByMemoryMbDescRow, error) {
	rows, err := q.db.Query(ctx, listTeamTemplatesByMemoryMbDesc,
		arg.TeamID,
		arg.IncludeDefaults,
		arg.CpuCount,
		arg.MemoryMb,
		arg.FilterPublic,
		arg.Search,
		arg.CursorMemoryMb,
		arg.CursorID,
		arg.LimitPlusOne,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListTeamTemplatesByMemoryMbDescRow
	for rows.Next() {
		var i ListTeamTemplatesByMemoryMbDescRow
		if err := rows.Scan(
			&i.TemplateID,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.Public,
			&i.BuildCount,
			&i.SpawnCount,
			&i.LastSpawnedAt,
			&i.CreatorID,
			&i.BuildID,
			&i.CpuCount,
			&i.MemoryMb,
			&i.DiskSizeMb,
			&i.EnvdVersion,
			&i.Aliases,
			&i.Names,
			&i.IsDefault,
			&i.DefaultDescription,
			&i.NameSortKey,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listTeamTemplatesByNameAsc = `-- name: ListTeamTemplatesByNameAsc :many

SELECT
    e.id AS template_id,
    e.created_at,
    e.updated_at,
    e.public,
    e.build_count,
    e.spawn_count,
    e.last_spawned_at,
    e.created_by AS creator_id,
    COALESCE(eb.id, '00000000-0000-0000-0000-000000000000'::uuid) AS build_id,
    COALESCE(eb.vcpu, 0)::bigint AS cpu_count,
    COALESCE(eb.ram_mb, 0)::bigint AS memory_mb,
    eb.total_disk_size_mb AS disk_size_mb,
    eb.envd_version AS envd_version,
    COALESCE(ea.aliases, ARRAY[]::text[])::text[] AS aliases,
    COALESCE(ea.names, ARRAY[]::text[])::text[] AS names,
    (d.env_id IS NOT NULL)::boolean AS is_default,
    d.description AS default_description,
    COALESCE(ea.name_sort_key, '')::text AS name_sort_key
FROM public.envs AS e
LEFT JOIN public.env_defaults AS d ON d.env_id = e.id
LEFT JOIN LATERAL (
    SELECT
        ARRAY_AGG(alias ORDER BY alias) AS aliases,
        ARRAY_AGG(CASE WHEN namespace IS NOT NULL THEN namespace || '/' || alias ELSE alias END ORDER BY alias) AS names,
        MIN(CASE WHEN namespace IS NOT NULL THEN namespace || '/' || alias ELSE alias END) AS name_sort_key
    FROM public.env_aliases
    WHERE env_id = e.id
) ea ON TRUE
LEFT JOIN LATERAL (
    SELECT b.id, b.vcpu, b.ram_mb, b.total_disk_size_mb, b.envd_version
    FROM public.env_build_assignments AS ba
    JOIN public.env_builds AS b ON b.id = ba.build_id
    WHERE ba.env_id = e.id AND ba.tag = 'default' AND b.status_group = 'ready'
    ORDER BY ba.created_at DESC
    LIMIT 1
) eb ON TRUE
WHERE
    (
        (e.team_id = $1::uuid AND e.source = 'template')
        OR ($2::boolean AND d.env_id IS NOT NULL)
    )
    AND ($3::bigint = 0 OR COALESCE(eb.vcpu, 0) = $3::bigint)
    AND ($4::bigint = 0 OR COALESCE(eb.ram_mb, 0) = $4::bigint)
    AND ($5::smallint = -1 OR e.public = ($5::smallint = 1))
    AND (
        $6::text = ''
        OR e.id ILIKE '%' || $6::text || '%'
        OR EXISTS (
            SELECT 1 FROM public.env_aliases sa
            WHERE sa.env_id = e.id
              AND (
                sa.alias ILIKE '%' || $6::text || '%'
                OR COALESCE(sa.namespace, '') || '/' || sa.alias ILIKE '%' || $6::text || '%'
              )
        )
    )
    AND (
        $7::text IS NULL
        OR COALESCE(ea.name_sort_key, '') > $7::text
        OR (COALESCE(ea.name_sort_key, '') = $7::text AND e.id > $8::text)
    )
ORDER BY COALESCE(ea.name_sort_key, '') ASC, e.id ASC
LIMIT $9::int
`

type ListTeamTemplatesByNameAscParams struct {
	TeamID          uuid.UUID
	IncludeDefaults bool
	CpuCount        int64
	MemoryMb        int64
	FilterPublic    int16
	Search          string
	CursorName      *string
	CursorID        *string
	LimitPlusOne    int32
}

type ListTeamTemplatesByNameAscRow struct {
	TemplateID         string
	CreatedAt          time.Time
	UpdatedAt          time.Time
	Public             bool
	BuildCount         int32
	SpawnCount         int64
	LastSpawnedAt      *time.Time
	CreatorID          *uuid.UUID
	BuildID            uuid.UUID
	CpuCount           int64
	MemoryMb           int64
	DiskSizeMb         *int64
	EnvdVersion        *string
	Aliases            []string
	Names              []string
	IsDefault          bool
	DefaultDescription *string
	NameSortKey        string
}

// Paginated list of a team's templates, optionally including default templates
// (env_defaults membership) inline when include_defaults is true. Filtering
// (cpu/memory/visibility) and name+id search are shared across every variant;
// sorting is split into one query per (column, direction) with a fixed ORDER BY
// and a keyset predicate keyed on the sort column + the env id tiebreak (always
// ascending). The cursor columns are nullable: a NULL cursor means the first
// page.
func (q *Queries) ListTeamTemplatesByNameAsc(ctx context.Context, arg ListTeamTemplatesByNameAscParams) ([]ListTeamTemplatesByNameAscRow, error) {
	rows, err := q.db.Query(ctx, listTeamTemplatesByNameAsc,
		arg.TeamID,
		arg.IncludeDefaults,
		arg.CpuCount,
		arg.MemoryMb,
		arg.FilterPublic,
		arg.Search,
		arg.CursorName,
		arg.CursorID,
		arg.LimitPlusOne,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListTeamTemplatesByNameAscRow
	for rows.Next() {
		var i ListTeamTemplatesByNameAscRow
		if err := rows.Scan(
			&i.TemplateID,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.Public,
			&i.BuildCount,
			&i.SpawnCount,
			&i.LastSpawnedAt,
			&i.CreatorID,
			&i.BuildID,
			&i.CpuCount,
			&i.MemoryMb,
			&i.DiskSizeMb,
			&i.EnvdVersion,
			&i.Aliases,
			&i.Names,
			&i.IsDefault,
			&i.DefaultDescription,
			&i.NameSortKey,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listTeamTemplatesByNameDesc = `-- name: ListTeamTemplatesByNameDesc :many
SELECT
    e.id AS template_id,
    e.created_at,
    e.updated_at,
    e.public,
    e.build_count,
    e.spawn_count,
    e.last_spawned_at,
    e.created_by AS creator_id,
    COALESCE(eb.id, '00000000-0000-0000-0000-000000000000'::uuid) AS build_id,
    COALESCE(eb.vcpu, 0)::bigint AS cpu_count,
    COALESCE(eb.ram_mb, 0)::bigint AS memory_mb,
    eb.total_disk_size_mb AS disk_size_mb,
    eb.envd_version AS envd_version,
    COALESCE(ea.aliases, ARRAY[]::text[])::text[] AS aliases,
    COALESCE(ea.names, ARRAY[]::text[])::text[] AS names,
    (d.env_id IS NOT NULL)::boolean AS is_default,
    d.description AS default_description,
    COALESCE(ea.name_sort_key, '')::text AS name_sort_key
FROM public.envs AS e
LEFT JOIN public.env_defaults AS d ON d.env_id = e.id
LEFT JOIN LATERAL (
    SELECT
        ARRAY_AGG(alias ORDER BY alias) AS aliases,
        ARRAY_AGG(CASE WHEN namespace IS NOT NULL THEN namespace || '/' || alias ELSE alias END ORDER BY alias) AS names,
        MIN(CASE WHEN namespace IS NOT NULL THEN namespace || '/' || alias ELSE alias END) AS name_sort_key
    FROM public.env_aliases
    WHERE env_id = e.id
) ea ON TRUE
LEFT JOIN LATERAL (
    SELECT b.id, b.vcpu, b.ram_mb, b.total_disk_size_mb, b.envd_version
    FROM public.env_build_assignments AS ba
    JOIN public.env_builds AS b ON b.id = ba.build_id
    WHERE ba.env_id = e.id AND ba.tag = 'default' AND b.status_group = 'ready'
    ORDER BY ba.created_at DESC
    LIMIT 1
) eb ON TRUE
WHERE
    (
        (e.team_id = $1::uuid AND e.source = 'template')
        OR ($2::boolean AND d.env_id IS NOT NULL)
    )
    AND ($3::bigint = 0 OR COALESCE(eb.vcpu, 0) = $3::bigint)
    AND ($4::bigint = 0 OR COALESCE(eb.ram_mb, 0) = $4::bigint)
    AND ($5::smallint = -1 OR e.public = ($5::smallint = 1))
    AND (
        $6::text = ''
        OR e.id ILIKE '%' || $6::text || '%'
        OR EXISTS (
            SELECT 1 FROM public.env_aliases sa
            WHERE sa.env_id = e.id
              AND (
                sa.alias ILIKE '%' || $6::text || '%'
                OR COALESCE(sa.namespace, '') || '/' || sa.alias ILIKE '%' || $6::text || '%'
              )
        )
    )
    AND (
        $7::text IS NULL
        OR COALESCE(ea.name_sort_key, '') < $7::text
        OR (COALESCE(ea.name_sort_key, '') = $7::text AND e.id > $8::text)
    )
ORDER BY COALESCE(ea.name_sort_key, '') DESC, e.id ASC
LIMIT $9::int
`

type ListTeamTemplatesByNameDescParams struct {
	TeamID          uuid.UUID
	IncludeDefaults bool
	CpuCount        int64
	MemoryMb        int64
	FilterPublic    int16
	Search          string
	CursorName      *string
	CursorID        *string
	LimitPlusOne    int32
}

type ListTeamTemplatesByNameDescRow struct {
	TemplateID         string
	CreatedAt          time.Time
	UpdatedAt          time.Time
	Public             bool
	BuildCount         int32
	SpawnCount         int64
	LastSpawnedAt      *time.Time
	CreatorID          *uuid.UUID
	BuildID            uuid.UUID
	CpuCount           int64
	MemoryMb           int64
	DiskSizeMb         *int64
	EnvdVersion        *string
	Aliases            []string
	Names              []string
	IsDefault          bool
	DefaultDescription *string
	NameSortKey        string
}

func (q *Queries) ListTeamTemplatesByNameDesc(ctx context.Context, arg ListTeamTemplatesByNameDescParams) ([]ListTeamTemplatesByNameDescRow, error) {
	rows, err := q.db.Query(ctx, listTeamTemplatesByNameDesc,
		arg.TeamID,
		arg.IncludeDefaults,
		arg.CpuCount,
		arg.MemoryMb,
		arg.FilterPublic,
		arg.Search,
		arg.CursorName,
		arg.CursorID,
		arg.LimitPlusOne,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListTeamTemplatesByNameDescRow
	for rows.Next() {
		var i ListTeamTemplatesByNameDescRow
		if err := rows.Scan(
			&i.TemplateID,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.Public,
			&i.BuildCount,
			&i.SpawnCount,
			&i.LastSpawnedAt,
			&i.CreatorID,
			&i.BuildID,
			&i.CpuCount,
			&i.MemoryMb,
			&i.DiskSizeMb,
			&i.EnvdVersion,
			&i.Aliases,
			&i.Names,
			&i.IsDefault,
			&i.DefaultDescription,
			&i.NameSortKey,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listTeamTemplatesByUpdatedAtAsc = `-- name: ListTeamTemplatesByUpdatedAtAsc :many
SELECT
    e.id AS template_id,
    e.created_at,
    e.updated_at,
    e.public,
    e.build_count,
    e.spawn_count,
    e.last_spawned_at,
    e.created_by AS creator_id,
    COALESCE(eb.id, '00000000-0000-0000-0000-000000000000'::uuid) AS build_id,
    COALESCE(eb.vcpu, 0)::bigint AS cpu_count,
    COALESCE(eb.ram_mb, 0)::bigint AS memory_mb,
    eb.total_disk_size_mb AS disk_size_mb,
    eb.envd_version AS envd_version,
    COALESCE(ea.aliases, ARRAY[]::text[])::text[] AS aliases,
    COALESCE(ea.names, ARRAY[]::text[])::text[] AS names,
    (d.env_id IS NOT NULL)::boolean AS is_default,
    d.description AS default_description,
    COALESCE(ea.name_sort_key, '')::text AS name_sort_key
FROM public.envs AS e
LEFT JOIN public.env_defaults AS d ON d.env_id = e.id
LEFT JOIN LATERAL (
    SELECT
        ARRAY_AGG(alias ORDER BY alias) AS aliases,
        ARRAY_AGG(CASE WHEN namespace IS NOT NULL THEN namespace || '/' || alias ELSE alias END ORDER BY alias) AS names,
        MIN(CASE WHEN namespace IS NOT NULL THEN namespace || '/' || alias ELSE alias END) AS name_sort_key
    FROM public.env_aliases
    WHERE env_id = e.id
) ea ON TRUE
LEFT JOIN LATERAL (
    SELECT b.id, b.vcpu, b.ram_mb, b.total_disk_size_mb, b.envd_version
    FROM public.env_build_assignments AS ba
    JOIN public.env_builds AS b ON b.id = ba.build_id
    WHERE ba.env_id = e.id AND ba.tag = 'default' AND b.status_group = 'ready'
    ORDER BY ba.created_at DESC
    LIMIT 1
) eb ON TRUE
WHERE
    (
        (e.team_id = $1::uuid AND e.source = 'template')
        OR ($2::boolean AND d.env_id IS NOT NULL)
    )
    AND ($3::bigint = 0 OR COALESCE(eb.vcpu, 0) = $3::bigint)
    AND ($4::bigint = 0 OR COALESCE(eb.ram_mb, 0) = $4::bigint)
    AND ($5::smallint = -1 OR e.public = ($5::smallint = 1))
    AND (
        $6::text = ''
        OR e.id ILIKE '%' || $6::text || '%'
        OR EXISTS (
            SELECT 1 FROM public.env_aliases sa
            WHERE sa.env_id = e.id
              AND (
                sa.alias ILIKE '%' || $6::text || '%'
                OR COALESCE(sa.namespace, '') || '/' || sa.alias ILIKE '%' || $6::text || '%'
              )
        )
    )
    AND (
        $7::timestamptz IS NULL
        OR e.updated_at > $7::timestamptz
        OR (e.updated_at = $7::timestamptz AND e.id > $8::text)
    )
ORDER BY e.updated_at ASC, e.id ASC
LIMIT $9::int
`

type ListTeamTemplatesByUpdatedAtAscParams struct {
	TeamID          uuid.UUID
	IncludeDefaults bool
	CpuCount        int64
	MemoryMb        int64
	FilterPublic    int16
	Search          string
	CursorUpdatedAt *time.Time
	CursorID        *string
	LimitPlusOne    int32
}

type ListTeamTemplatesByUpdatedAtAscRow struct {
	TemplateID         string
	CreatedAt          time.Time
	UpdatedAt          time.Time
	Public             bool
	BuildCount         int32
	SpawnCount         int64
	LastSpawnedAt      *time.Time
	CreatorID          *uuid.UUID
	BuildID            uuid.UUID
	CpuCount           int64
	MemoryMb           int64
	DiskSizeMb         *int64
	EnvdVersion        *string
	Aliases            []string
	Names              []string
	IsDefault          bool
	DefaultDescription *string
	NameSortKey        string
}

func (q *Queries) ListTeamTemplatesByUpdatedAtAsc(ctx context.Context, arg ListTeamTemplatesByUpdatedAtAscParams) ([]ListTeamTemplatesByUpdatedAtAscRow, error) {
	rows, err := q.db.Query(ctx, listTeamTemplatesByUpdatedAtAsc,
		arg.TeamID,
		arg.IncludeDefaults,
		arg.CpuCount,
		arg.MemoryMb,
		arg.FilterPublic,
		arg.Search,
		arg.CursorUpdatedAt,
		arg.CursorID,
		arg.LimitPlusOne,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListTeamTemplatesByUpdatedAtAscRow
	for rows.Next() {
		var i ListTeamTemplatesByUpdatedAtAscRow
		if err := rows.Scan(
			&i.TemplateID,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.Public,
			&i.BuildCount,
			&i.SpawnCount,
			&i.LastSpawnedAt,
			&i.CreatorID,
			&i.BuildID,
			&i.CpuCount,
			&i.MemoryMb,
			&i.DiskSizeMb,
			&i.EnvdVersion,
			&i.Aliases,
			&i.Names,
			&i.IsDefault,
			&i.DefaultDescription,
			&i.NameSortKey,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listTeamTemplatesByUpdatedAtDesc = `-- name: ListTeamTemplatesByUpdatedAtDesc :many
SELECT
    e.id AS template_id,
    e.created_at,
    e.updated_at,
    e.public,
    e.build_count,
    e.spawn_count,
    e.last_spawned_at,
    e.created_by AS creator_id,
    COALESCE(eb.id, '00000000-0000-0000-0000-000000000000'::uuid) AS build_id,
    COALESCE(eb.vcpu, 0)::bigint AS cpu_count,
    COALESCE(eb.ram_mb, 0)::bigint AS memory_mb,
    eb.total_disk_size_mb AS disk_size_mb,
    eb.envd_version AS envd_version,
    COALESCE(ea.aliases, ARRAY[]::text[])::text[] AS aliases,
    COALESCE(ea.names, ARRAY[]::text[])::text[] AS names,
    (d.env_id IS NOT NULL)::boolean AS is_default,
    d.description AS default_description,
    COALESCE(ea.name_sort_key, '')::text AS name_sort_key
FROM public.envs AS e
LEFT JOIN public.env_defaults AS d ON d.env_id = e.id
LEFT JOIN LATERAL (
    SELECT
        ARRAY_AGG(alias ORDER BY alias) AS aliases,
        ARRAY_AGG(CASE WHEN namespace IS NOT NULL THEN namespace || '/' || alias ELSE alias END ORDER BY alias) AS names,
        MIN(CASE WHEN namespace IS NOT NULL THEN namespace || '/' || alias ELSE alias END) AS name_sort_key
    FROM public.env_aliases
    WHERE env_id = e.id
) ea ON TRUE
LEFT JOIN LATERAL (
    SELECT b.id, b.vcpu, b.ram_mb, b.total_disk_size_mb, b.envd_version
    FROM public.env_build_assignments AS ba
    JOIN public.env_builds AS b ON b.id = ba.build_id
    WHERE ba.env_id = e.id AND ba.tag = 'default' AND b.status_group = 'ready'
    ORDER BY ba.created_at DESC
    LIMIT 1
) eb ON TRUE
WHERE
    (
        (e.team_id = $1::uuid AND e.source = 'template')
        OR ($2::boolean AND d.env_id IS NOT NULL)
    )
    AND ($3::bigint = 0 OR COALESCE(eb.vcpu, 0) = $3::bigint)
    AND ($4::bigint = 0 OR COALESCE(eb.ram_mb, 0) = $4::bigint)
    AND ($5::smallint = -1 OR e.public = ($5::smallint = 1))
    AND (
        $6::text = ''
        OR e.id ILIKE '%' || $6::text || '%'
        OR EXISTS (
            SELECT 1 FROM public.env_aliases sa
            WHERE sa.env_id = e.id
              AND (
                sa.alias ILIKE '%' || $6::text || '%'
                OR COALESCE(sa.namespace, '') || '/' || sa.alias ILIKE '%' || $6::text || '%'
              )
        )
    )
    AND (
        $7::timestamptz IS NULL
        OR e.updated_at < $7::timestamptz
        OR (e.updated_at = $7::timestamptz AND e.id > $8::text)
    )
ORDER BY e.updated_at DESC, e.id ASC
LIMIT $9::int
`

type ListTeamTemplatesByUpdatedAtDescParams struct {
	TeamID          uuid.UUID
	IncludeDefaults bool
	CpuCount        int64
	MemoryMb        int64
	FilterPublic    int16
	Search          string
	CursorUpdatedAt *time.Time
	CursorID        *string
	LimitPlusOne    int32
}

type ListTeamTemplatesByUpdatedAtDescRow struct {
	TemplateID         string
	CreatedAt          time.Time
	UpdatedAt          time.Time
	Public             bool
	BuildCount         int32
	SpawnCount         int64
	LastSpawnedAt      *time.Time
	CreatorID          *uuid.UUID
	BuildID            uuid.UUID
	CpuCount           int64
	MemoryMb           int64
	DiskSizeMb         *int64
	EnvdVersion        *string
	Aliases            []string
	Names              []string
	IsDefault          bool
	DefaultDescription *string
	NameSortKey        string
}

func (q *Queries) ListTeamTemplatesByUpdatedAtDesc(ctx context.Context, arg ListTeamTemplatesByUpdatedAtDescParams) ([]ListTeamTemplatesByUpdatedAtDescRow, error) {
	rows, err := q.db.Query(ctx, listTeamTemplatesByUpdatedAtDesc,
		arg.TeamID,
		arg.IncludeDefaults,
		arg.CpuCount,
		arg.MemoryMb,
		arg.FilterPublic,
		arg.Search,
		arg.CursorUpdatedAt,
		arg.CursorID,
		arg.LimitPlusOne,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListTeamTemplatesByUpdatedAtDescRow
	for rows.Next() {
		var i ListTeamTemplatesByUpdatedAtDescRow
		if err := rows.Scan(
			&i.TemplateID,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.Public,
			&i.BuildCount,
			&i.SpawnCount,
			&i.LastSpawnedAt,
			&i.CreatorID,
			&i.BuildID,
			&i.CpuCount,
			&i.MemoryMb,
			&i.DiskSizeMb,
			&i.EnvdVersion,
			&i.Aliases,
			&i.Names,
			&i.IsDefault,
			&i.DefaultDescription,
			&i.NameSortKey,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
