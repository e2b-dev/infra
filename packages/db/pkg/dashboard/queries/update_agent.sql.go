// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: update_agent.sql

package dashboardqueries

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const updateAgent = `-- name: UpdateAgent :one
UPDATE public.agents
SET
    name = CASE WHEN $1::boolean THEN $2::text ELSE name END,
    template_id = CASE WHEN $3::boolean THEN $4::text ELSE template_id END,
    description = CASE WHEN $5::boolean THEN $6::text ELSE description END,
    command = CASE WHEN $7::boolean THEN $8::text ELSE command END,
    author = CASE WHEN $9::boolean THEN $10::text ELSE author END,
    metadata = CASE WHEN $11::boolean THEN COALESCE($12::text::jsonb, '{}'::jsonb) ELSE metadata END,
    public = CASE WHEN $13::boolean THEN $14::boolean ELSE public END,
    published_at = CASE WHEN $15::boolean THEN $16::timestamptz ELSE published_at END,
    updated_at = now()
WHERE id = $17::uuid
  AND team_id = $18::uuid
  AND deleted_at IS NULL
RETURNING
    id,
    team_id,
    name,
    template_id,
    description,
    command,
    author,
    metadata,
    public,
    published_at,
    created_at,
    updated_at,
    deleted_at
`

type UpdateAgentParams struct {
	NameSet        bool
	Name           string
	TemplateIDSet  bool
	TemplateID     string
	DescriptionSet bool
	Description    string
	CommandSet     bool
	Command        *string
	AuthorSet      bool
	Author         *string
	MetadataSet    bool
	Metadata       *string
	PublicSet      bool
	Public         bool
	PublishedAtSet bool
	PublishedAt    *time.Time
	ID             uuid.UUID
	TeamID         uuid.UUID
}

type UpdateAgentRow struct {
	ID          uuid.UUID
	TeamID      *uuid.UUID
	Name        string
	TemplateID  string
	Description string
	Command     *string
	Author      *string
	Metadata    []byte
	Public      bool
	PublishedAt *time.Time
	CreatedAt   time.Time
	UpdatedAt   time.Time
	DeletedAt   *time.Time
}

func (q *Queries) UpdateAgent(ctx context.Context, arg UpdateAgentParams) (UpdateAgentRow, error) {
	row := q.db.QueryRow(ctx, updateAgent,
		arg.NameSet,
		arg.Name,
		arg.TemplateIDSet,
		arg.TemplateID,
		arg.DescriptionSet,
		arg.Description,
		arg.CommandSet,
		arg.Command,
		arg.AuthorSet,
		arg.Author,
		arg.MetadataSet,
		arg.Metadata,
		arg.PublicSet,
		arg.Public,
		arg.PublishedAtSet,
		arg.PublishedAt,
		arg.ID,
		arg.TeamID,
	)
	var i UpdateAgentRow
	err := row.Scan(
		&i.ID,
		&i.TeamID,
		&i.Name,
		&i.TemplateID,
		&i.Description,
		&i.Command,
		&i.Author,
		&i.Metadata,
		&i.Public,
		&i.PublishedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.DeletedAt,
	)
	return i, err
}
