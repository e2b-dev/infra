// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: get_team_member_ids.sql

package authqueries

import (
	"context"

	"github.com/google/uuid"
)

const getTeamMemberIDs = `-- name: GetTeamMemberIDs :many
SELECT ut.user_id
FROM "public"."users_teams" ut
WHERE ut.team_id = $1
`

// GetTeamMemberIDs lists the users whose cached auth entry carries this team's
// data, so invalidating the team can reach all of them.
func (q *Queries) GetTeamMemberIDs(ctx context.Context, teamID uuid.UUID) ([]uuid.UUID, error) {
	rows, err := q.db.Query(ctx, getTeamMemberIDs, teamID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []uuid.UUID
	for rows.Next() {
		var user_id uuid.UUID
		if err := rows.Scan(&user_id); err != nil {
			return nil, err
		}
		items = append(items, user_id)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
