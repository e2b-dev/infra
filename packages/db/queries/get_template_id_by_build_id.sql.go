// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: get_template_id_by_build_id.sql

package queries

import (
	"context"

	"github.com/google/uuid"
)

const getTemplateIDByBuildID = `-- name: GetTemplateIDByBuildID :one
SELECT env_id
FROM public.env_build_assignments
WHERE build_id = $1::uuid
ORDER BY created_at DESC, id DESC
LIMIT 1
`

// Returns the template (env) id that owns the given build, picking the
// most recent assignment if multiple exist (e.g. a tag was reassigned).
// Used by the skipIfUnchanged short-circuit to look up the prior
// snapshot's template id without re-running the full upsert. See
// issue e2b-dev/infra#2580.
func (q *Queries) GetTemplateIDByBuildID(ctx context.Context, buildID uuid.UUID) (string, error) {
	row := q.db.QueryRow(ctx, getTemplateIDByBuildID, buildID)
	var env_id string
	err := row.Scan(&env_id)
	return env_id, err
}
