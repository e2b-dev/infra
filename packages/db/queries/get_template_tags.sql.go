// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: get_template_tags.sql

package queries

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const getTemplateTags = `-- name: GetTemplateTags :many
SELECT DISTINCT ON (ba.tag) ba.tag, ba.build_id, ba.created_at
FROM public.env_build_assignments AS ba
WHERE ba.env_id = $1
ORDER BY ba.tag, ba.created_at DESC
`

type GetTemplateTagsRow struct {
	Tag       string
	BuildID   uuid.UUID
	CreatedAt pgtype.Timestamptz
}

// Lists all tags for a template, with the most recent build assignment for each tag.
func (q *Queries) GetTemplateTags(ctx context.Context, templateID string) ([]GetTemplateTagsRow, error) {
	rows, err := q.db.Query(ctx, getTemplateTags, templateID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetTemplateTagsRow
	for rows.Next() {
		var i GetTemplateTagsRow
		if err := rows.Scan(&i.Tag, &i.BuildID, &i.CreatedAt); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
