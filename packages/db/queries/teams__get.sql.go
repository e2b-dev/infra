// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: teams__get.sql

package queries

import (
	"context"

	"github.com/google/uuid"
)

const getTeamWithTierByAPIKeyWithUpdateLastUsed = `-- name: GetTeamWithTierByAPIKeyWithUpdateLastUsed :one
UPDATE "public"."team_api_keys" tak
SET last_used = now()
FROM "public"."teams" t
         JOIN "public"."tiers" tier ON t.tier = tier.id
         LEFT JOIN LATERAL (
    SELECT SUM(concurrent_sandboxes)       as concurrent_sandboxes,
           SUM(concurrent_template_builds) as concurrent_template_builds,
           SUM(vcpu)                       as vcpu,
           SUM(ram_mb)                     as ram_mb,
           coalesce(MAX(disk_mb),0)        as disk_mb,
           SUM(max_vcpu)                   as max_vcpu
    FROM "public"."addons" addon
    WHERE addon.team_id = t.id
      AND addon.valid_from <= now()
      AND (addon.valid_to IS NULL OR addon.valid_to > now())
    ) addons ON true
WHERE tak.team_id = t.id
  AND tak.api_key_hash = $1
RETURNING t.id, t.created_at, t.is_blocked, t.name, t.tier, t.email, t.is_banned, t.blocked_reason, t.cluster_id, tier.id, tier.name, tier.disk_mb, tier.concurrent_instances, tier.max_length_hours, tier.max_vcpu, tier.max_ram_mb, tier.concurrent_template_builds, addons.concurrent_sandboxes, addons.concurrent_template_builds, addons.vcpu, addons.ram_mb, addons.disk_mb, addons.max_vcpu
`

type GetTeamWithTierByAPIKeyWithUpdateLastUsedRow struct {
	Team  Team
	Tier  Tier
	Addon Addon
}

func (q *Queries) GetTeamWithTierByAPIKeyWithUpdateLastUsed(ctx context.Context, apiKeyHash string) (GetTeamWithTierByAPIKeyWithUpdateLastUsedRow, error) {
	row := q.db.QueryRow(ctx, getTeamWithTierByAPIKeyWithUpdateLastUsed, apiKeyHash)
	var i GetTeamWithTierByAPIKeyWithUpdateLastUsedRow
	err := row.Scan(
		&i.Team.ID,
		&i.Team.CreatedAt,
		&i.Team.IsBlocked,
		&i.Team.Name,
		&i.Team.Tier,
		&i.Team.Email,
		&i.Team.IsBanned,
		&i.Team.BlockedReason,
		&i.Team.ClusterID,
		&i.Tier.ID,
		&i.Tier.Name,
		&i.Tier.DiskMb,
		&i.Tier.ConcurrentInstances,
		&i.Tier.MaxLengthHours,
		&i.Tier.MaxVcpu,
		&i.Tier.MaxRamMb,
		&i.Tier.ConcurrentTemplateBuilds,
		&i.Addon.ID,
		&i.Addon.Name,
		&i.Addon.Description,
		&i.Addon.ConcurrentInstances,
		&i.Addon.MaxLengthHours,
		&i.Addon.ConcurrentTemplateBuilds,
		&i.Addon.MaxVcpu,
		&i.Addon.MaxRamMb,
		&i.Addon.DiskMb,
		&i.Addon.TeamID,
		&i.Addon.ValidFrom,
		&i.Addon.ValidTo,
		&i.Addon.AddedBy,
	)
	return i, err
}

const getTeamWithTierByTeamAndUser = `-- name: GetTeamWithTierByTeamAndUser :one
SELECT t.id, t.created_at, t.is_blocked, t.name, t.tier, t.email, t.is_banned, t.blocked_reason, t.cluster_id, tier.id, tier.name, tier.disk_mb, tier.concurrent_instances, tier.max_length_hours, tier.max_vcpu, tier.max_ram_mb, tier.concurrent_template_builds, addons.concurrent_sandboxes, addons.concurrent_template_builds, addons.vcpu, addons.ram_mb, addons.disk_mb, addons.max_vcpu
FROM "public"."teams" t
JOIN "public"."tiers" tier ON t.tier = tier.id
JOIN "public"."users_teams" ut ON ut.team_id = t.id
LEFT JOIN LATERAL (
    SELECT SUM(concurrent_sandboxes)       as concurrent_sandboxes,
           SUM(concurrent_template_builds) as concurrent_template_builds,
           SUM(vcpu)                       as vcpu,
           SUM(ram_mb)                     as ram_mb,
           coalesce(MAX(disk_mb),0)        as disk_mb,
           SUM(max_vcpu)                   as max_vcpu
    FROM "public"."addons" addon
    WHERE addon.team_id = t.id
      AND addon.valid_from <= now()
      AND (addon.valid_to IS NULL OR addon.valid_to > now())
    ) addons ON true
WHERE ut.user_id = $1 AND t.id = $2
`

type GetTeamWithTierByTeamAndUserParams struct {
	UserID uuid.UUID
	ID     uuid.UUID
}

type GetTeamWithTierByTeamAndUserRow struct {
	Team  Team
	Tier  Tier
	Addon Addon
}

func (q *Queries) GetTeamWithTierByTeamAndUser(ctx context.Context, arg GetTeamWithTierByTeamAndUserParams) (GetTeamWithTierByTeamAndUserRow, error) {
	row := q.db.QueryRow(ctx, getTeamWithTierByTeamAndUser, arg.UserID, arg.ID)
	var i GetTeamWithTierByTeamAndUserRow
	err := row.Scan(
		&i.Team.ID,
		&i.Team.CreatedAt,
		&i.Team.IsBlocked,
		&i.Team.Name,
		&i.Team.Tier,
		&i.Team.Email,
		&i.Team.IsBanned,
		&i.Team.BlockedReason,
		&i.Team.ClusterID,
		&i.Tier.ID,
		&i.Tier.Name,
		&i.Tier.DiskMb,
		&i.Tier.ConcurrentInstances,
		&i.Tier.MaxLengthHours,
		&i.Tier.MaxVcpu,
		&i.Tier.MaxRamMb,
		&i.Tier.ConcurrentTemplateBuilds,
		&i.Addon.ID,
		&i.Addon.Name,
		&i.Addon.Description,
		&i.Addon.ConcurrentInstances,
		&i.Addon.MaxLengthHours,
		&i.Addon.ConcurrentTemplateBuilds,
		&i.Addon.MaxVcpu,
		&i.Addon.MaxRamMb,
		&i.Addon.DiskMb,
		&i.Addon.TeamID,
		&i.Addon.ValidFrom,
		&i.Addon.ValidTo,
		&i.Addon.AddedBy,
	)
	return i, err
}

const getTeamsWithUsersTeamsWithTier = `-- name: GetTeamsWithUsersTeamsWithTier :many
SELECT t.id, t.created_at, t.is_blocked, t.name, t.tier, t.email, t.is_banned, t.blocked_reason, t.cluster_id, ut.id, ut.user_id, ut.team_id, ut.is_default, ut.added_by, ut.created_at, tier.id, tier.name, tier.disk_mb, tier.concurrent_instances, tier.max_length_hours, tier.max_vcpu, tier.max_ram_mb, tier.concurrent_template_builds, addons.concurrent_sandboxes, addons.concurrent_template_builds, addons.vcpu, addons.ram_mb, addons.disk_mb, addons.max_vcpu
FROM "public"."teams" t
         JOIN "public"."tiers" tier ON t.tier = tier.id
         JOIN "public"."users_teams" ut ON ut.team_id = t.id
         LEFT JOIN LATERAL (
    SELECT SUM(concurrent_sandboxes)       as concurrent_sandboxes,
           SUM(concurrent_template_builds) as concurrent_template_builds,
           SUM(vcpu)                       as vcpu,
           SUM(ram_mb)                     as ram_mb,
           coalesce(MAX(disk_mb),0)        as disk_mb,
           SUM(max_vcpu)                   as max_vcpu
    FROM "public"."addons" addon
    WHERE addon.team_id = t.id
      AND addon.valid_from <= now()
      AND (addon.valid_to IS NULL OR addon.valid_to > now())
    ) addons ON true
WHERE ut.user_id = $1
`

type GetTeamsWithUsersTeamsWithTierRow struct {
	Team      Team
	UsersTeam UsersTeam
	Tier      Tier
	Addon     Addon
}

func (q *Queries) GetTeamsWithUsersTeamsWithTier(ctx context.Context, userID uuid.UUID) ([]GetTeamsWithUsersTeamsWithTierRow, error) {
	rows, err := q.db.Query(ctx, getTeamsWithUsersTeamsWithTier, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetTeamsWithUsersTeamsWithTierRow
	for rows.Next() {
		var i GetTeamsWithUsersTeamsWithTierRow
		if err := rows.Scan(
			&i.Team.ID,
			&i.Team.CreatedAt,
			&i.Team.IsBlocked,
			&i.Team.Name,
			&i.Team.Tier,
			&i.Team.Email,
			&i.Team.IsBanned,
			&i.Team.BlockedReason,
			&i.Team.ClusterID,
			&i.UsersTeam.ID,
			&i.UsersTeam.UserID,
			&i.UsersTeam.TeamID,
			&i.UsersTeam.IsDefault,
			&i.UsersTeam.AddedBy,
			&i.UsersTeam.CreatedAt,
			&i.Tier.ID,
			&i.Tier.Name,
			&i.Tier.DiskMb,
			&i.Tier.ConcurrentInstances,
			&i.Tier.MaxLengthHours,
			&i.Tier.MaxVcpu,
			&i.Tier.MaxRamMb,
			&i.Tier.ConcurrentTemplateBuilds,
			&i.Addon.ID,
			&i.Addon.Name,
			&i.Addon.Description,
			&i.Addon.ConcurrentInstances,
			&i.Addon.MaxLengthHours,
			&i.Addon.ConcurrentTemplateBuilds,
			&i.Addon.MaxVcpu,
			&i.Addon.MaxRamMb,
			&i.Addon.DiskMb,
			&i.Addon.TeamID,
			&i.Addon.ValidFrom,
			&i.Addon.ValidTo,
			&i.Addon.AddedBy,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
