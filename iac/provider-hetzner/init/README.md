# Hetzner Init Module

Bootstrap module for Hetzner-based E2B-Stack.

Usage:
```bash
terraform init
terraform plan -var-file=../.env.hetzner.tfvars
terraform apply -var-file=../.env.hetzner.tfvars
```
